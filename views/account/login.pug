extends ../layout

block content
  .pb-2.mt-2.mb-4.border-bottom
    h3 Sign in
  form(method='POST')
    input(type='hidden', name='_csrf', value=_csrf)
    .form-group
      .offset-md-3.col-md-7.pl-2
        a.btn.btn-block.btn-bottlepay.btn-social(href='/auth/bottlepay')
          i.fab.fa-bitcoin.fa-sm
          | Sign in with BottlePay
